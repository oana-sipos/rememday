<%= form_for(@entry) do |f| %>
  <% if @entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@entry.errors.count, "error") %> prohibited this entry from being saved:</h2>

      <ul>
      <% @entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>:<%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :memory %>:<%= f.text_field :memory %>
  </div>
  <div class="field">
    <%= f.label :mood %>:<%= f.select :mood, options_for_select([1,2,3,4,5]), include_blank: :true %>
  </div>
  <div class="field">
    <%= f.label :weight %>:<%= f.text_field :weight%>
  </div>
  <div class="actions">
    <%= f.submit class: "btn btn-primary btn-sm" %>
  </div>
<% end %>
